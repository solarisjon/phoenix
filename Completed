[main 8063e93] Project view redesign: status-grouped sections, objective, AI suggest, full history Backend: - Migration 034: add objective TEXT column to projects table - model.Project: Objective field with json tag - store: projectSelectCols, Create, Update, scanProject, scanProjects all updated to include objective - store: ListProjectHistory returns all completed tasks for a project regardless of dismissed state (new TaskRepo method + interface entry) - API: createProjectRequest / createProject / updateProject accept objective - API: GET /api/projects/:id/history — full completed task history - API: POST /api/projects/:id/suggest — LLM-powered next-action suggestion (uses first available LLM provider, falls back gracefully) Frontend: - api.ts: Project type gains objective field; projects.history() and projects.suggest() added - ProjectsWorkspace: add phoenixWS import and useRef - MiddlePane fully rewritten: * Inline-editable objective block (click to edit, Enter to save, Escape to cancel, persisted via PUT /api/projects/:id) * Status counts (done/failed/running/attention) as colored badges * ✦ Suggest button → AI suggestion card with Run this / dismiss * Tasks grouped into collapsible sections in priority order: Needs Attention * Completed section collapsed by default; collapse state persisted in localStorage per project * Completed section loads full history (incl. dismissed tasks) on first expand via GET /api/projects/:id/history * WS subscription on task.status_changed refreshes task list live so sections update without manual reload * Empty state surfaces both + Task and ✦ Suggest
 13 files changed, 932 insertions(+), 136 deletions(-)
 create mode 100644 internal/store/sqlite/migrations/034_project_objective.sql
 create mode 100644 phoenix-architecture.drawio
 create mode 100644 phoenix-architecture.drawio.png
 create mode 100644 phoenix-architecture.png
