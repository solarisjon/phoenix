# a running list of things to implement resolve etc
# See docs/superpowers/specs/CONTEXT.md for full project context (read this first when resuming)
#
  - [FIXED] failed tasks need a retry option — POST /api/tasks/:id/retry, retry buttons on task cards, inbox, dashboard
  - [FIXED] inbox now shows failed + awaiting_approval tasks, grouped by state, with project name + clickable project links
  - [FIXED] dashboard task activity rows are clickable — opens detail modal with output + retry for failed tasks
  - [FIXED] agent model_override field — per-agent model that overrides provider default at execution time
  - [FIXED] AI generate button on agent form — POST /api/agents/generate returns persona/instructions/guardrails from a description
  - [FIXED] dismiss tasks from inbox — POST /api/tasks/:id/dismiss hides from inbox without deleting
  - [FIXED] edit tasks from inbox — title + description editable on non-running tasks
  - [FIXED] Needs Attention stat card links to /inbox
  - [FIXED] Tasks Running stat card opens live running-tasks panel (card grid with streamed output)
  - [FIXED] Agent can_spawn_agents flag (off by default) — spawn endpoint + system prompt injection
  - [FIXED] Project working directory — per-project path passed to coding agents as working dir
  - [FIXED] Project deletion — safe delete with running-task guard, cascade removes tasks+assignments
  - [FIXED] Heartbeat scheduling — scheduler fires periodic tasks for agents with heartbeat_interval set
  - [TODO]  model picker dropdown — list available models per adapter kind
  - [TODO]  README — professional project documentation
  - [TODO]  database backup/restore — export and reimport the SQLite DB
  - [TODO]  import/export agents and teams — share agent configs between instances
  - [TODO]  UI theming
  - [TODO]  agent teams — group agents into a named team, assign team to project
  - need to think of a way managing heartbeat projects , projects may be static where there are created and a human adds tasks etc some of those tasks may need refining so we should allow for a completed task toi be refined. But also some tasks like manage a queue should wake and generate a persistent task on an agents queue so when they wake they can actioin it .. i am open to suggestions as to how best presnet these flows and make it intuitive as to whats going on whats pending and whats been completed





## coding agent adapter issues (in progress)
  - [FIXED] old binary was still running (07:32 build, pre-opencode support) — kill & restart required
  - [FIXED] DB provider configs had wrong shape (args_template/working_directory) from old UI — updated via sqlite3
  - [FIXED] registry only knew about opencode kind — now dispatches pi and claudecode kinds too
  - [FIXED] pi adapter written (internal/provider/pi/) — streams text_delta events, all tests pass
  - [FIXED] claudecode adapter written (internal/provider/claudecode/) — streams assistant message blocks
  - [FIXED] UI CodingAgentFields updated — kind dropdown now has opencode / pi / Claude Code with per-kind fields
  - [FIXED] smoke test passed — pi adapter ran task, completed in ~3s, returned correct output
             route is POST /api/tasks with {project_id, agent_id, title, description} — run triggered automatically
  - [TODO]  pi adapter: no_session defaults to false in DB records — update existing PI provider config or handle missing field gracefully
  - [TODO]  claudecode adapter: needs smoke test once claude auth is set up


