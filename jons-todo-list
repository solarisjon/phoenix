# Phoenix — backlog
# All work items are now tracked as GitHub issues.
# https://github.com/solarisjon/phoenix/issues

## Open
# #13  Mobile-friendly layout (sidebar collapses to bottom nav on narrow screens)
# #19  Keyboard shortcuts (R=retry, D=dismiss, J/K=navigate, Enter=open detail)
# #12  Multi-user authentication (Phase 7 in design doc)

## Blocked (need external setup)
# #7   Copilot CLI adapter — needs `copilot login` interactive setup
# #14  claudecode smoke test — needs `claude` CLI authenticated
